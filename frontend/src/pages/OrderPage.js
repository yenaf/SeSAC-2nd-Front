import React from 'react';

// 주문 번호가 랜덤으로 생성된다(숫자+문자 10자리)
// 현재년도(숫자4자리) + 카테고리문자(문자1자리) + 랜덤숫자 5자리

// 결제 페이지
export default function OrderPage() {
  return (
    <div>
      <h2>주문서</h2>
    </div>
  );
}
